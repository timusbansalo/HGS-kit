

copy run_freakout.txt to Freakout installation folder. 
rename it to run_freakout.bat 
Open admin command window and above script can be run to monitor CPU frequency.

Ignore below:
=============
Install and run freakout to view current clocks.

Then open the flops counter and go through the options SSE, AVX2, AVX512. Make note of the current frequencies each time. 
Measure single thread, as well as add thread to max CPU threads and make note of frequencies.

By the way, these are both custom Intel internal tools. Do not distribute publicly.